{% extends "layout.jinja" %} {% from 'sections/ui/breadcrumb.jinja' import breadcrumb %} {% block content %}
<div class="container">
  <!-- Breadcrumb Navigation -->
  {{ breadcrumb([
        {'name': 'Home', 'url': '/'},
        {'name': 'Dresses', 'url': '/dresses'},
        {'name': product.name or 'Linen dress'}
    ]) }}

  <!-- Product Detail Grid -->
  <div class="row">
    <!-- Left Grid: Image Slider -->
    <div class="col-lg-6">{% include 'sections/product/media-gallery/gallery.jinja' %}</div>

    <!-- Right Grid: Product Information -->
    <div class="col-lg-6">
      <div class="product-info">
        {% include 'sections/product/product-info/title-vendor.jinja' %} {% include
        'sections/product/product-info/variant-selection.jinja' %} {% include
        'sections/product/product-info/promotion-list.jinja' %}
      </div>
    </div>
  </div>

  <!-- Product Details Section -->
  <div class="row mt-5">
    <div class="col-12">{% include 'sections/product/details/specs-accordion.jinja' %}</div>
  </div>

  <!-- Customer Reviews Section -->
  <div class="row mt-2">
    <div class="col-12">{% include 'sections/product/reviews/reviews-summary.jinja' %}</div>
  </div>

  <!-- Related Products Section -->
  <div class="row mt-2">
    <div class="col-12">{% include 'sections/product/related-products/related-products.jinja' %}</div>
  </div>
</div>
{% endblock %}
